enum State {
    COMPLETE
    INCOMPLETE
    IN_PROGRESS
  }

type Todo {
  id: ID!
  title: String
  state: State
}

type Query {
  todos: [Todo]
  todo(id: ID!): Todo
}

type Mutation {
  createTodo(title: String!, state: State!): Todo
}

schema {
    query: Query
    mutation: Mutation
}